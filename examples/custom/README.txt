
Before running this example, be sure to run the "fetch_data.sh" script in 
the parent directory.  That will download about ~50MB of data needed for 
this example.  Then you can do (for example):

$> python3 custom_example.py

or

$> mpirun -np 4 python3 custom_example.py

